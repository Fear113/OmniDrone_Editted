from shortuuid import random
from stable_baselines3 import  PPO
from ~~~ import MAPPOPolicy

model = MAPPOPolicy.load()

obs = np.,random.randn(())
act = model.predict(obs)
<10ms - (f, r, p, y) or (f, r_rate)
<20